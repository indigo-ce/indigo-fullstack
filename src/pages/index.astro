---
// The default locale (English) page - should work without language prefix
// For non-English locales, the middleware will redirect to the appropriate path

// Import the same components as the dynamic route
import {useTranslations} from "@/i18n/utils";
import {defaultLocale} from "@/i18n/constants";
import Layout from "@/layouts/Layout.astro";
import {Icon, CircleStack, PaintBrush, Key, Server} from "svelte-hero-icons";

// Use the default locale
const lang = defaultLocale;
const t = useTranslations(lang);
---

<Layout title={t.home.title}>
  <div class="py-24 sm:py-32">
    <div class="mx-auto max-w-7xl px-6 lg:px-8">
      <div class="mx-auto max-w-2xl lg:text-center">
        <span
          class="text-xs/7 font-mono text-indigo-600 dark:text-indigo-400 border border-indigo-600 dark:border-indigo-400 rounded-full px-2 py-1"
        >
          {t.common.version}
        </span>
        <p
          class="mt-2 text-4xl font-semibold tracking-tight text-pretty text-gray-900 dark:text-white sm:text-5xl lg:text-balance"
        >
          {t.home.title}
        </p>
        <p class="mt-6 text-lg/8 text-gray-600 dark:text-gray-300">
          {t.home.description}
        </p>
      </div>
    </div>
    <div class="mx-auto mt-16 max-w-2xl px-4 sm:mt-20 lg:mt-24 lg:max-w-4xl">
      <dl
        class="grid max-w-xl grid-cols-1 gap-x-8 gap-y-10 lg:max-w-none lg:grid-cols-2 lg:gap-y-16"
      >
        <div class="relative pl-16">
          <dt class="text-base/7 font-semibold text-gray-900 dark:text-white">
            <div
              class="absolute top-0 left-0 flex size-10 items-center justify-center rounded-full bg-pink-600"
            >
              <Icon src={Server} class="size-6 text-white" solid />
            </div>
            {t.home.feature1.title}
          </dt>
          <dd class="mt-2 text-base/7 text-muted-foreground">
            {t.home.feature1.description}
          </dd>
        </div>
        <div class="relative pl-16">
          <dt class="text-base/7 font-semibold text-gray-900 dark:text-white">
            <div
              class="absolute top-0 left-0 flex size-10 items-center justify-center rounded-full bg-amber-500"
            >
              <Icon src={PaintBrush} class="size-6 text-white" solid />
            </div>
            {t.home.feature2.title}
          </dt>
          <dd class="mt-2 text-base/7 text-muted-foreground">
            {t.home.feature2.description}
          </dd>
        </div>
        <div class="relative pl-16">
          <dt class="text-base/7 font-semibold text-gray-900 dark:text-white">
            <div
              class="absolute top-0 left-0 flex size-10 items-center justify-center rounded-full bg-blue-600"
            >
              <Icon src={Key} class="size-6 text-white" solid />
            </div>
            {t.home.feature3.title}
          </dt>
          <dd class="mt-2 text-base/7 text-muted-foreground">
            {t.home.feature3.description}
          </dd>
        </div>
        <div class="relative pl-16">
          <dt class="text-base/7 font-semibold text-gray-900 dark:text-white">
            <div
              class="absolute top-0 left-0 flex size-10 items-center justify-center rounded-full bg-lime-600"
            >
              <Icon src={CircleStack} class="size-6 text-white" solid />
            </div>
            {t.home.feature4.title}
          </dt>
          <dd class="mt-2 text-base/7 text-muted-foreground">
            {t.home.feature4.description}
          </dd>
        </div>
      </dl>
    </div>
  </div>
</Layout>
