---
export interface Props {
  user: import("better-auth").User;
}

const {user} = Astro.props;
---

<div
  class="bg-card border border-border rounded-[var(--radius)] p-6 mb-6 shadow-sm"
>
  <h2 class="text-lg text-foreground mt-0 mb-2">
    Hello, <span class="font-bold text-primary">{user.name}</span>
  </h2>
  <p class="text-sm text-muted-foreground m-0">
    Welcome back to your dashboard. Your email is <strong>{user.email}</strong>.
  </p>
</div>
